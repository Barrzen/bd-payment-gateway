# bd-payment-gateway-core

Shared core primitives for `bd-payment-gateway` provider crates.

Includes:

- `PaymentProvider` trait
- common payment types
- `Environment` resolution
- redaction-safe HTTP runtime
- unified error model (`BdPaymentError`)
